package interface_study;

public interface Constructor_interface {

	//public Constructor_interface() {
		
	//}      java do no allow  creation o constructor because even if we create constructor in interface as incomplete method,but we have to override it 
	//but constructor is not possible to override therefore java at first stage  dont allow us to create constructor in interface
}
